"""Shared test fixtures."""

# Tests currently exercise pure helpers (JSON parsing, YAML validation,
# JD scoring) that don't touch the database. When integration tests land,
# add a Postgres fixture here.
