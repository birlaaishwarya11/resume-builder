# Future Improvements

- [ ] Update the frontend UX for rules and databases
- [ ] Make settings an overlay (instead of a full page)
- [ ] Onboarding review: "+ Add Section" button. Inline form asking for a
      section name (free text) and a section type (Bullets / Entries / Skills).
      Appends a new custom-section card to the review form, persists as a
      `custom_sections` entry on save, and renders in the PDF + appears in
      Settings. Scope matches Resume.io / Zety / Kickresume "extra section"
      flows. Out of scope for this item: user-defined fields inside entries
      (needs renderer changes first).
